DATABASE_URL=postgres://${app_username}:${app_password}@${database_address}:${database_port}/${database_name}
ROOT_DATABASE_URL=postgres://${database_username}:${database_password}@${database_address}:${database_port}/${database_name}
S3_BUCKET_REGION=${aws_region}
S3_BUCKET_NAME=${s3_bucket_name}

JWT_PUBLIC_KEY=${jwt_public_key}
JWT_PRIVATE_KEY=${jwt_private_key}

SSL_CERT=${ssl_cert}
SSL_KEY=${ssl_key}

GOOGLE_REFRESH_TOKEN=${google_refresh_token}

INSTALLED_GOOGLE_CLIENT_ID=${google_credentials_installed.client_id}
INSTALLED_GOOGLE_CLIENT_SECRET=${google_credentials_installed.client_secret}
INSTALLED_GOOGLE_REDIRECT_URI=${google_credentials_installed.redirect_uri}

WEB_GOOGLE_CLIENT_ID=${google_credentials_web.client_id}
WEB_GOOGLE_CLIENT_SECRET=${google_credentials_web.client_secret}
WEB_GOOGLE_REDIRECT_URI=${google_credentials_web.redirect_uri}